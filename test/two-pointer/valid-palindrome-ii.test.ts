import { validPalindrome } from '../../src/two-pointer/valid-palindrome-ii';

describe('valid palindrome ii', () => {
  test('valid palindrome ii - test case 1', async () => {
    expect(validPalindrome('aba')).toBe(true);
  });
});
